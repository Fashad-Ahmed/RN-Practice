import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import TodoApp from '../src/features/TodoApp';
const Contact = () => {
  return (
    <View>
      <TodoApp />
    </View>
  );
};

export default Contact;

const styles = StyleSheet.create({});
