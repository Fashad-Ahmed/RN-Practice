import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const todoReducer = () => {
  return (
    <View>
      <Text>todoReducer</Text>
    </View>
  );
};

export default todoReducer;

const styles = StyleSheet.create({});
